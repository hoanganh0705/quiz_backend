# Backend env inventory (`quiz_backend`)

> Generated by **ET-1.5-B1** — Epic 1.5, Batch B.
> Cross-check: every key declared by `quiz_backend/src/core/config/env.validation.ts` (lines 209–294) vs. existing `quiz_backend/.env.example`.

## Summary

- **Keys declared by `validateEnv()`:** 28
- **Keys present in `.env.example`:** 26
- **Missing from `.env.example`:** 2 → queue for ET-1.5-B2 (`EMAIL_SEND_TIMEOUT_MS`, `EMAIL_QUEUE_CONCURRENCY`).
- **Keys present in `.env.example` but NOT declared by `validateEnv()`:** 4 — read elsewhere in the codebase; noted for context only, out of scope for this inventory.

## Inventory (declared by `validateEnv()`)

| # | Variable | Declared type | Required? | In `.env.example`? | Current `.env.example` value | Notes |
|---|---|---|---|---|---|---|
| 1 | `DATABASE_URL` | URL (`postgres:` / `postgresql:`) | **required** | yes | `postgres://postgres:postgres@localhost:5432/quizdb` | |
| 2 | `REDIS_URL` | URL (`redis:` / `rediss:`) | **required** | yes | `redis://localhost:6379` | |
| 3 | `JWT_ACCESS_TOKEN_SECRET` | non-empty string | **required** | yes | `<generate-a-random-secret>` | sample placeholder; not a real secret |
| 4 | `JWT_REFRESH_TOKEN_SECRET` | non-empty string | **required** | yes | `<generate-a-different-random-secret>` | sample placeholder |
| 5 | `ACCESS_TOKEN_EXPIRES_IN` | `<n>[smhd]?` | **required** | yes | `15m` | |
| 6 | `REFRESH_TOKEN_EXPIRES_IN` | `<n>[smhd]?` | **required** | yes | `7d` | |
| 7 | `REFRESH_TOKEN_COOKIE_MAX_AGE_MS` | positive integer | **required** (no fallback) | yes | `604800000` | |
| 8 | `MAX_ACTIVE_SESSIONS_PER_USER` | positive integer | optional (fallback `5`) | yes | `5` | matches fallback |
| 9 | `REFRESH_TOKEN_REUSE_GRACE_WINDOW_SECONDS` | positive integer | optional (fallback `10`) | yes | `10` | matches fallback |
| 10 | `EMAIL_VERIFICATION_TOKEN_TTL_SECONDS` | positive integer | optional (fallback `1800`) | yes | `3600` | overrides fallback |
| 11 | `JWT_ACCESS_TOKEN_ISSUER` | non-empty string | **required** | yes | `quiz-backend` | |
| 12 | `JWT_ACCESS_TOKEN_AUDIENCE` | non-empty string | **required** | yes | `quiz-client` | |
| 13 | `SESSION_BINDING_STRICT` | boolean | optional (fallback `false`) | yes | `false` | matches fallback |
| 14 | `TRUST_PROXY` | boolean | optional (fallback `false`) | yes | `true` | overrides fallback |
| 15 | `PORT` | positive integer | optional (fallback `3000`) | yes | `8080` | overrides fallback |
| 16 | `NODE_ENV` | enum: `development`/`test`/`production` | **required** | yes | `development` | |
| 17 | `CORS_ORIGINS` | string (CSV) | optional (defaults to `""`) | yes | `http://localhost:3000,http://localhost:5173` | |
| 18 | `EMAIL_VERIFICATION_BASE_URL` | string | optional (defaults to `""`) | yes | `http://localhost:8080/api/auth/verify-email` | |
| 19 | `EMAIL_PROVIDER` | enum: `resend` | **required** | yes | `resend` | |
| 20 | `EMAIL_FROM_ADDRESS` | non-empty string | **required** | yes | `onboarding@resend.dev` | |
| 21 | `EMAIL_FROM_NAME` | non-empty string | **required** | yes | `Quiz App Support` | |
| 22 | `RESEND_API_KEY` | non-empty string | **required** | yes | `re_<your-api-key-from-resend-dashboard>` | sample placeholder |
| 23 | **`EMAIL_SEND_TIMEOUT_MS`** | positive integer | optional (fallback `5000`) | **NO — missing** | — | **→ ET-1.5-B2** |
| 24 | **`EMAIL_QUEUE_CONCURRENCY`** | positive integer | optional (fallback `5`) | **NO — missing** | — | **→ ET-1.5-B2** |
| 25 | `APP_NAME` | string | optional (default `Quiz API`) | yes | `Quiz API` | matches default |
| 26 | `APP_VERSION` | string | optional (default `1.0`) | yes | `1.0` | matches default |
| 27 | `APP_DESCRIPTION` | string | optional (default `""`) | yes | `REST API for the quiz application` | overrides default |
| 28 | `APP_URL` | string | optional (default `""`) | yes | `http://localhost:8080` | overrides default |

## Missing rows (queue for ET-1.5-B2)

1. **`EMAIL_SEND_TIMEOUT_MS`** — declared at `env.validation.ts:248`, fallback `5000`.
2. **`EMAIL_QUEUE_CONCURRENCY`** — declared at `env.validation.ts:249`, fallback `5`.

## Out-of-validation keys present in `.env.example` (context only, out of scope)

| Variable | Comment |
|---|---|
| `PASSWORD_RESET_TOKEN_TTL_SECONDS` | Read elsewhere; not declared by `validateEnv()`. |
| `PASSWORD_RESET_BASE_URL` | Read elsewhere; not declared by `validateEnv()`. |
| `SWAGGER_ENABLED` | Read elsewhere; not declared by `validateEnv()`. |
| `GOOGLE_CLIENT_ID` | Read elsewhere; not declared by `validateEnv()`. |

These are noted for completeness. ET-1.5-B1 makes no recommendation on them; ET-1.5-B2 only acts on the 2 missing keys from `validateEnv()`.